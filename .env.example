
# 加密上游订阅 URL、分享令牌与 API Key。
# 可留空：首次启动会在 data/crypto.key 自动生成并持久化随机密钥。
# 已有部署请继续使用原密钥，不要清空或更换，否则已有加密数据无法解密。
ENCRYPTION_KEY=

# development=本机开发；production=正式部署
APP_ENV=development

# 监听地址
HTTP_ADDR=:8080

# 时区（默认 Asia/Shanghai；影响日志时间戳、按日切分等本地时间）
TZ=Asia/Shanghai
