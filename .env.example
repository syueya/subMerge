# Geo 数据和订阅源 User-Agent、超时、刷新间隔、日志等应用配置请在「设置 → 系统设置」中修改。
# 这些网页设置保存在 SQLite，不再读取同名环境变量。

# 加密上游订阅 URL、分享令牌与 API Key。
# 可留空：首次启动会在 data/crypto.key 自动生成并持久化随机密钥。
# 已有部署请继续使用原密钥，不要清空或更换，否则已有加密数据无法解密。
ENCRYPTION_KEY=

# 对外访问根地址、可信反向代理和 Cookie Secure 请在「设置 → 系统设置」中修改

# development=本机开发；production=正式部署
APP_ENV=development

# 监听地址
HTTP_ADDR=:8080

# 可信反向代理的 IP/CIDR、会话 Cookie Secure 和公开访问地址请在「设置 → 系统设置」中修改。
# TRUSTED_PROXIES、COOKIE_SECURE 不再从环境变量读取。

# 时区（默认 Asia/Shanghai；影响日志时间戳、按日切分等本地时间）
TZ=Asia/Shanghai
