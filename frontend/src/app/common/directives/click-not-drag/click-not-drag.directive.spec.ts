import { ClickNotDragDirective } from './click-not-drag.directive';

describe('ClickNotDragDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickNotDragDirective();
    expect(directive).toBeTruthy();
  });
});
